import { Box, Typography } from "@mui/material";

export default function Calendar() {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4">Calendar</Typography>
      <Typography>Meetings & scheduling UI.</Typography>
    </Box>
  );
}
